{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for Sentris.",
  "windows": ["main"],
  "permissions": ["core:default", "shell:default"]
}

